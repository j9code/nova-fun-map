name: Update PlayNoVA GeoJSON Data

on:
  schedule:
    # Run at 10:00 UTC daily (5:00 AM EST)
    - cron: "0 10 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Fetch and convert data
        run: python3 scripts/fetch.py
        env:
          PLAYNOVA_DROP_THRESHOLD: ${{ vars.DROP_THRESHOLD || '50' }}
          PLAYNOVA_MAX_DATA_LAG_HOURS: ${{ vars.MAX_DATA_LAG_HOURS || '48' }}

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/novafunmap.geojson
          git diff --staged --quiet && echo "No changes to commit." || (git commit -m "Update novafunmap.geojson" && git push)

